import Header from "@/app/components/Header";
import Paragraph from "@/app/components/Paragraph";

export default function AddingOnClick() {
    return (
        <article id="adding-onclick">
            <Header>Adding onClick Handler to Buttons</Header>

            <Paragraph>In this section, you will learn how to handle user events such as button clicking.</Paragraph>
        </article>
    );
}